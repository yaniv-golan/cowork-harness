// Ordinary code — used only to prove skill-b's files are reached when the TARGET is the plugin root,
// but excluded when the target is skill-a specifically (sibling-skill exclusion).
export function noop() {
  return true;
}
